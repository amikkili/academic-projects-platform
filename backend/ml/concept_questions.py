# Viva question knowledge base — concept → difficulty → [{ q, a }]
# Each concept has 6 easy, 6 medium, 5 hard questions minimum.
# Questions are shuffled per-user using seeded RNG in viva_gen.py.

CONCEPT_QA = {

    # ─── SENTIMENT ANALYSIS ────────────────────────────────────────────────────

    "lstm": {
        "easy": [
            {"q": "What does LSTM stand for?",
             "a": "Long Short-Term Memory — a special type of Recurrent Neural Network designed to learn long-range dependencies in sequential data."},
            {"q": "What problem in standard RNNs does LSTM solve?",
             "a": "The vanishing gradient problem. During backpropagation through many time steps, gradients shrink to near zero in RNNs. LSTM's gating mechanism prevents this."},
            {"q": "How many gates does an LSTM cell have? Name them.",
             "a": "Three gates: the forget gate (decides what to discard from memory), the input gate (decides what new info to store), and the output gate (decides what to output)."},
            {"q": "What is the cell state in an LSTM?",
             "a": "The cell state is the long-term memory that runs through the entire sequence. It carries information across time steps with minimal transformations, unlike the hidden state."},
            {"q": "What type of output does the final Dense layer produce in binary sentiment classification?",
             "a": "A single value between 0 and 1 using the sigmoid activation function — 0 is negative sentiment, 1 is positive."},
            {"q": "What dataset is used in this project and how many reviews does it contain?",
             "a": "The IMDB movie reviews dataset — 50,000 labeled reviews split equally into 25,000 training and 25,000 test samples."},
        ],
        "medium": [
            {"q": "Explain how the forget gate and input gate work together to update the cell state.",
             "a": "The forget gate (f_t) multiplies the previous cell state C_{t-1} to erase irrelevant info. The input gate (i_t) multiplies a candidate cell state (C̃_t) to add new info. New cell state: C_t = f_t * C_{t-1} + i_t * C̃_t."},
            {"q": "Why is LSTM preferred over a simple RNN for this sentiment analysis task?",
             "a": "Movie reviews can be long (100-200 words). Simple RNNs forget early context by the time they reach the end. LSTM maintains a cell state that preserves long-range dependencies like negation ('not good') even across many tokens."},
            {"q": "What is the difference between the hidden state (h_t) and cell state (C_t) in LSTM?",
             "a": "The hidden state h_t is the short-term output passed to the next time step and to the output layer — it's what the network 'currently thinks'. The cell state C_t is long-term memory that flows through with minimal changes, carrying context across many steps."},
            {"q": "What does `dropout=0.2, recurrent_dropout=0.2` do in the LSTM layer in this project?",
             "a": "dropout=0.2 applies 20% dropout on the inputs to the LSTM, and recurrent_dropout=0.2 applies 20% dropout on the recurrent connections between time steps. Both prevent overfitting by randomly zeroing units during training."},
            {"q": "In this project, what is the purpose of `pad_sequences` and what value fills shorter sequences?",
             "a": "All sequences must be the same length (max_len=200) for batch processing. pad_sequences pads shorter reviews with zeros at the beginning by default (pre-padding), ensuring uniform input shape [batch, 200]."},
            {"q": "If the model predicts 0.73 for a review, what is the classification and how confident is it?",
             "a": "The review is classified as positive (>0.5 threshold). The model is 73% confident it is positive. In practice, predictions above 0.7 or below 0.3 are considered high-confidence."},
        ],
        "hard": [
            {"q": "Explain mathematically why LSTM avoids the vanishing gradient better than a standard RNN.",
             "a": "In RNNs, gradients flow through repeated matrix multiplications (∂h_t/∂h_{t-k} ≈ W^k) — if |W| < 1, gradients vanish. LSTM's cell state update is additive (C_t = f*C_{t-1} + i*C̃_t), so the gradient of the loss w.r.t. C_{t-k} flows through additions rather than multiplications, keeping gradients from vanishing."},
            {"q": "How would you modify this architecture to perform 3-class sentiment (positive/neutral/negative)?",
             "a": "Change the final Dense layer from Dense(1, activation='sigmoid') to Dense(3, activation='softmax'). Change the loss from binary_crossentropy to categorical_crossentropy (one-hot labels) or sparse_categorical_crossentropy (integer labels). Adjust the label encoding accordingly."},
            {"q": "Compare LSTM with GRU. Would GRU work better for this project?",
             "a": "GRU has 2 gates (reset, update) vs LSTM's 3, so fewer parameters and faster training. For IMDB sentiment (medium-length sequences), GRU often matches LSTM accuracy. For this project, GRU is a valid and efficient alternative — LSTM may have marginal accuracy advantage on very long texts."},
            {"q": "What does `return_sequences=True` do and when would you need it in a stacked LSTM?",
             "a": "By default LSTM returns only the last hidden state h_T. With return_sequences=True it returns h_t for every time step [batch, timesteps, units]. Needed when stacking LSTM layers, because the second LSTM expects a full sequence, not a single vector."},
            {"q": "How would you handle out-of-vocabulary (OOV) words at inference time?",
             "a": "Keras tokenizer reserves index 1 for OOV words by default (num_words=10000 keeps the 10,000 most common words). At inference, unseen words map to index 1, whose embedding is trained as a generic OOV vector. Better: use subword tokenization (BPE) or pre-trained embeddings (GloVe) to handle rare words."},
        ],
    },

    "word_embeddings": {
        "easy": [
            {"q": "What is a word embedding?",
             "a": "A dense vector representation of a word in continuous space, where semantically similar words are close to each other. For example, 'good' and 'great' have similar embedding vectors."},
            {"q": "What is the embedding dimension used in this project?",
             "a": "128 dimensions — each word is represented as a vector of 128 floating-point numbers."},
            {"q": "What is the vocabulary size in this project?",
             "a": "10,000 — only the 10,000 most frequent words in the training corpus are kept. All others become OOV."},
            {"q": "Why is one-hot encoding unsuitable for large vocabularies in NLP?",
             "a": "One-hot encoding creates a vector of size V (vocabulary size). With 10,000 words, each word is a 10,000-D sparse vector with a single 1. This is memory-intensive and captures no semantic similarity between words."},
            {"q": "Are the embeddings in this project pre-trained or learned from scratch?",
             "a": "Learned from scratch on the IMDB training data. Pre-trained embeddings like GloVe or Word2Vec are trained on much larger corpora and often give better results, especially with limited training data."},
            {"q": "What is the output shape of the Embedding layer given max_len=200 and embed_dim=128?",
             "a": "[batch_size, 200, 128] — for each sample in the batch, each of the 200 tokens gets a 128-D vector."},
        ],
        "medium": [
            {"q": "What is the difference between Word2Vec Skip-gram and CBOW models?",
             "a": "Skip-gram predicts context words given a center word — better for rare words. CBOW predicts the center word from surrounding context words — faster to train. Both learn embeddings as a byproduct of the prediction task."},
            {"q": "How do you integrate pre-trained GloVe embeddings into this Keras model?",
             "a": "Load the GloVe vectors into a matrix indexed by word index from the tokenizer. Pass it as embedding_initializer to the Embedding layer and optionally set trainable=False to freeze them: Embedding(vocab_size, 300, weights=[embedding_matrix], trainable=False)."},
            {"q": "What does it mean when the embedding matrix has `trainable=True` (default)?",
             "a": "The embedding vectors are updated during backpropagation just like any other weight. The model fine-tunes them for the specific task (sentiment classification), which can improve performance if training data is sufficient."},
            {"q": "How would you visualize learned word embeddings to verify quality?",
             "a": "Extract the embedding weights after training, apply PCA or t-SNE to reduce to 2D, and plot. Quality embeddings cluster semantically related words: positive words ('great', 'excellent', 'amazing') near each other, negative words ('terrible', 'awful', 'horrible') in another cluster."},
            {"q": "What is the masking=True parameter in Keras Embedding and why would you use it?",
             "a": "masking=True tells downstream layers (like LSTM) to ignore padded timesteps (index 0). Without it, the LSTM processes zero-padded positions as real inputs, which adds noise and wastes computation. Enables proper variable-length sequence handling."},
        ],
        "hard": [
            {"q": "Explain the distributional hypothesis and how it underlies word embeddings.",
             "a": "Harris's distributional hypothesis: words that appear in similar contexts have similar meanings. Word2Vec exploits this by training a neural network to predict context from word (Skip-gram) or word from context (CBOW). The hidden layer weights become the embeddings — words in similar contexts get similar weights."},
            {"q": "What is subword tokenization (BPE) and why does it handle OOV words better than word-level tokenization?",
             "a": "BPE (Byte Pair Encoding) splits words into frequent subword units. 'unbelievable' → ['un', 'believe', 'able']. Each subword has an embedding. Even if 'unbelievable' was never seen, its subwords likely were — so OOV meaning is approximated from known subword embeddings. Used in BERT, GPT."},
            {"q": "How does the attention mechanism overcome the limitation of LSTM for very long documents?",
             "a": "LSTM compresses an entire sequence into a fixed-size hidden state — information about early tokens is progressively diluted. Attention mechanisms allow the model to directly access any token's representation when making predictions, bypassing the bottleneck. Transformers rely entirely on attention with no recurrence."},
            {"q": "What is the difference between static embeddings (Word2Vec, GloVe) and contextual embeddings (BERT, ELMo)?",
             "a": "Static embeddings assign one fixed vector per word regardless of context. 'bank' (financial) and 'bank' (river) get the same vector. Contextual embeddings (BERT) produce different vectors for the same word based on surrounding context, capturing polysemy. BERT uses transformer self-attention to build context-aware representations."},
        ],
    },

    "nlp_preprocessing": {
        "easy": [
            {"q": "What is tokenization in NLP?",
             "a": "Splitting a text into individual units (tokens) — usually words or subwords. For example, 'I loved this movie!' → ['I', 'loved', 'this', 'movie', '!']."},
            {"q": "What does `pad_sequences(X, maxlen=200)` do to a sequence of length 50?",
             "a": "Pads it to length 200 by adding 150 zeros at the beginning (pre-padding by default) → [0, 0, ..., 0, token1, token2, ..., token50]."},
            {"q": "What does `pad_sequences` do to a sequence longer than maxlen?",
             "a": "It truncates the sequence to maxlen, keeping the last maxlen tokens by default (post-truncation)."},
            {"q": "Why do we limit the vocabulary to 10,000 words?",
             "a": "Very rare words (typos, proper nouns) appear so infrequently they don't help the model learn and increase memory. 10,000 words cover the most meaningful vocabulary for sentiment while keeping the model lightweight."},
            {"q": "What does the Keras Tokenizer's `fit_on_texts` method do?",
             "a": "It scans all training texts, counts word frequencies, and builds a word-to-integer index sorted by frequency. The most common word gets index 1, next gets 2, etc. (Index 0 is reserved for padding.)"},
        ],
        "medium": [
            {"q": "What is the difference between pre-padding and post-padding, and which is better for LSTM?",
             "a": "Pre-padding adds zeros at the start; post-padding adds them at the end. For LSTM, pre-padding is generally better because the real content is processed last — the hidden state at the final time step reflects the actual review, not the padded zeros."},
            {"q": "Why is it important to fit the tokenizer only on training data and not the test data?",
             "a": "Fitting on test data would create data leakage — the model (or preprocessing) has seen information from the test set. Word frequencies from test data shouldn't influence the vocabulary built for training. In practice, test words not in the vocabulary just map to OOV."},
            {"q": "What preprocessing steps would improve model accuracy beyond what this project implements?",
             "a": "Lowercase normalization, HTML tag removal (IMDB reviews sometimes contain HTML), stopword removal (debated for sentiment — 'not' is important!), lemmatization, and handling contractions ('don't' → 'do not')."},
            {"q": "Why might removing stopwords actually hurt sentiment analysis performance?",
             "a": "Stopwords like 'not', 'very', 'but', 'never' are critical for sentiment. 'Not good' reversed by removing 'not' becomes 'good' — completely wrong. Most sentiment models keep stopwords for this reason."},
        ],
        "hard": [
            {"q": "Explain the difference between stemming and lemmatization with examples relevant to sentiment analysis.",
             "a": "Stemming chops word endings heuristically: 'loving' → 'lov' (may not be a valid word). Lemmatization maps to the dictionary base form using morphological analysis: 'loving' → 'love'. Lemmatization is more accurate but slower. For sentiment, 'loved'→'love' and 'loves'→'love' helps generalization."},
            {"q": "What is the Bag of Words model and what information does it lose that LSTM captures?",
             "a": "BoW represents text as word counts, losing word order completely. 'I don't love this movie' and 'I love this movie, I don't' have the same BoW. LSTM processes tokens sequentially, capturing word order and context — critical for negation, intensifiers ('very bad'), and discourse structure."},
            {"q": "How would you handle multilingual reviews if this model were deployed in a real product?",
             "a": "Options: (1) Language detection + separate models per language. (2) Multilingual embeddings (mBERT, LaBSE) that map multiple languages to a shared space. (3) Machine translation to English first. For production, multilingual transformers are now standard."},
        ],
    },

    # ─── STUDENT RESULT PORTAL ─────────────────────────────────────────────────

    "rest_api": {
        "easy": [
            {"q": "What does REST stand for?",
             "a": "Representational State Transfer — an architectural style for designing networked applications using HTTP methods."},
            {"q": "What are the four main HTTP methods used in a REST API and what does each do?",
             "a": "GET (retrieve data), POST (create new resource), PUT/PATCH (update existing resource), DELETE (remove resource)."},
            {"q": "What is an HTTP status code? Give examples for success and error.",
             "a": "A 3-digit code indicating the result of a request. 200 OK (success), 201 Created (resource created), 400 Bad Request (client error), 401 Unauthorized, 404 Not Found, 500 Internal Server Error."},
            {"q": "What is JSON and why is it used in REST APIs?",
             "a": "JavaScript Object Notation — a lightweight, human-readable format for exchanging data. It's language-independent, easy to parse, and natively supported in JavaScript (frontend), making it the standard for API communication."},
            {"q": "What is CORS and why might you encounter it in this project?",
             "a": "Cross-Origin Resource Sharing — a browser security mechanism. When the React frontend (port 3000) calls the Express API (port 5000), the browser blocks it unless the server explicitly sets CORS headers allowing that origin."},
            {"q": "What does a RESTful API endpoint like GET /api/students/:id do?",
             "a": "Retrieves the student resource with the specified ID from the server. The :id is a URL parameter substituted with the actual student's ID at runtime, e.g. GET /api/students/42."},
        ],
        "medium": [
            {"q": "Explain the difference between stateless and stateful APIs. Why is REST stateless?",
             "a": "Stateless: each request contains all info needed to process it (auth token, parameters). The server stores no session. Stateful: server remembers client state between requests. REST is stateless for scalability — any server can handle any request, enabling load balancing and horizontal scaling."},
            {"q": "What is the difference between PUT and PATCH methods?",
             "a": "PUT replaces the entire resource — you must send all fields. PATCH updates only the provided fields — partial updates. Example: PATCH /students/1 with {grade: 'A'} updates only the grade, keeping other fields unchanged."},
            {"q": "How do you implement pagination in a REST API for listing all students?",
             "a": "Use query parameters: GET /api/students?page=2&limit=10 returns 10 students starting from offset 20. The response includes metadata: { data: [...], total: 150, page: 2, pages: 15 }. Prevents loading all records at once."},
            {"q": "What is request body validation and how would you implement it in Express.js?",
             "a": "Validate incoming request data before processing. Use libraries like express-validator or joi: define rules (email must be valid, marks must be 0-100), check them as middleware, and return 400 with error details if validation fails."},
            {"q": "What is middleware in Express.js and give two examples used in this project?",
             "a": "Functions that execute between receiving a request and sending a response. Examples: auth middleware (verifies JWT token, adds user to req.user), CORS middleware (adds Access-Control headers), body-parser (parses JSON request bodies), error-handling middleware."},
        ],
        "hard": [
            {"q": "Explain REST API versioning strategies and which you would choose for this student portal.",
             "a": "Three approaches: (1) URL versioning: /api/v1/students — most visible, easy to route. (2) Header versioning: Accept: application/vnd.myapi.v1+json — cleaner URLs. (3) Query param: /api/students?version=1. URL versioning is recommended for this project for simplicity and debuggability."},
            {"q": "What is idempotency and which HTTP methods are idempotent?",
             "a": "A request is idempotent if making it multiple times produces the same result. GET, PUT, DELETE are idempotent. POST is not — calling POST /students twice creates two students. This matters for retry logic in client code."},
            {"q": "How would you design the API to handle role-based access control (admin vs student)?",
             "a": "Decode the JWT and attach user.role to the request. In route middleware, check req.user.role: if role is 'student', only allow access to their own data. If 'admin', allow all. Return 403 Forbidden for unauthorized role access. Define role checks as reusable middleware."},
            {"q": "What are the tradeoffs between returning detailed error messages vs. generic ones?",
             "a": "Detailed errors help debugging but expose implementation details (table names, library versions) to attackers. Production APIs should return generic messages to clients ('Invalid credentials') while logging detailed errors server-side. Never return stack traces to the client."},
        ],
    },

    "jwt_auth": {
        "easy": [
            {"q": "What does JWT stand for and what is it used for?",
             "a": "JSON Web Token — a compact, self-contained token used for authentication and authorization. It lets the server verify a user's identity without storing session data."},
            {"q": "What are the three parts of a JWT?",
             "a": "Header (algorithm used, e.g. HS256), Payload (claims — user id, role, expiry), and Signature (HMAC of header+payload using a secret key). Separated by dots: xxxxx.yyyyy.zzzzz."},
            {"q": "What is the difference between authentication and authorization?",
             "a": "Authentication verifies identity — 'who are you?' (login with email+password). Authorization determines permissions — 'what can you do?' (only admins can delete records). JWT handles authentication; role checks handle authorization."},
            {"q": "Where is the JWT sent in API requests after login?",
             "a": "In the Authorization header: Authorization: Bearer <token>. The server extracts and verifies it on protected routes before processing the request."},
            {"q": "What happens when a JWT expires?",
             "a": "The server returns 401 Unauthorized. The client must re-login or use a refresh token to obtain a new access token. Expiry is checked against the 'exp' claim in the payload."},
            {"q": "Why is JWT called 'stateless' authentication?",
             "a": "The server doesn't store sessions — all user info (id, role, expiry) is inside the token itself. Any server that knows the secret key can validate it, enabling horizontal scaling without shared session storage."},
        ],
        "medium": [
            {"q": "What is the difference between access tokens and refresh tokens?",
             "a": "Access token: short-lived (15-60 min), sent with every request. Refresh token: long-lived (7-30 days), stored securely (httpOnly cookie), used only to get new access tokens. Short-lived access tokens minimize damage if stolen; refresh tokens enable seamless UX without frequent re-logins."},
            {"q": "How do you invalidate a JWT before it expires? Why is this challenging?",
             "a": "JWTs are stateless — the server can't 'revoke' them by default. Solutions: (1) Token blacklist (store invalidated tokens in Redis, check on each request — adds state). (2) Short expiry times. (3) Rotate signing keys (invalidates all tokens — disruptive). This is a fundamental JWT tradeoff."},
            {"q": "What information should NOT be stored in the JWT payload and why?",
             "a": "Passwords, sensitive personal data (SSN, card numbers), or large objects. JWT payload is base64-encoded, NOT encrypted — anyone can decode it. Only store minimal non-sensitive claims: user_id, role, email. Use HTTPS to prevent interception."},
            {"q": "What is the difference between HS256 and RS256 signing algorithms in JWT?",
             "a": "HS256 uses a single shared secret key for signing and verification. RS256 uses RSA key pair: private key signs, public key verifies. RS256 is better for distributed systems — servers can verify without sharing the secret signing key."},
            {"q": "How would you implement role-based access in this student portal?",
             "a": "Store role in JWT payload: { user_id: 42, role: 'admin' }. Middleware extracts and verifies the token, adds user to req.user. Protected routes check req.user.role: admin can see all students, student can only see their own data. Return 403 Forbidden for unauthorized access."},
        ],
        "hard": [
            {"q": "Explain a JWT token forgery attack and how the signature prevents it.",
             "a": "Without the secret key, an attacker cannot forge a valid signature. If they change the payload (e.g., change role to 'admin'), the signature verification fails (HMAC(new_payload) ≠ original_signature). Never use `alg: none` — some early JWT libraries accepted unsigned tokens, which is a critical vulnerability."},
            {"q": "What is CSRF and does using JWT in Authorization headers (not cookies) protect against it?",
             "a": "CSRF (Cross-Site Request Forgery) tricks a browser into making authenticated requests. Cookies are sent automatically by the browser on any request to that domain — CSRF exploits this. JWTs in Authorization headers are not sent automatically, so they're CSRF-safe. However, if stored in cookies, add SameSite=Strict and CSRF tokens."},
            {"q": "Design a secure JWT auth flow for this portal including refresh token rotation.",
             "a": "Login → issue short-lived access token (15 min) + long-lived refresh token (7 days, httpOnly cookie). Each API request: verify access token. On expiry: client sends refresh token → server validates, issues new access token AND rotates refresh token (old one invalid). On logout: invalidate refresh token in DB. On suspicious reuse of old refresh token: invalidate entire refresh token family (detect token theft)."},
        ],
    },

    "mongodb": {
        "easy": [
            {"q": "What type of database is MongoDB?",
             "a": "A NoSQL document database — it stores data as JSON-like documents (BSON) rather than rows in relational tables."},
            {"q": "What is a collection in MongoDB? How does it compare to a SQL table?",
             "a": "A collection is a group of MongoDB documents, analogous to a table in SQL. Unlike SQL tables, collections have no fixed schema — documents in the same collection can have different fields."},
            {"q": "What is a document in MongoDB?",
             "a": "A JSON-like record (stored as BSON internally). Example: { _id: ObjectId('...'), name: 'Anil', marks: [85, 90, 78], grade: 'A' }. Documents can contain nested objects and arrays."},
            {"q": "What is the _id field in MongoDB?",
             "a": "A unique identifier automatically added to every document. By default it's an ObjectId — a 12-byte value encoding timestamp, machine ID, and a random counter. Serves as the primary key."},
            {"q": "What is Mongoose and why is it used with MongoDB in this project?",
             "a": "Mongoose is an ODM (Object Data Modeling) library for MongoDB and Node.js. It provides schema definition, validation, query building, and middleware — adding structure to MongoDB's schemaless nature."},
            {"q": "What is the difference between SQL and NoSQL databases?",
             "a": "SQL: structured tables, fixed schema, ACID transactions, powerful JOINs. NoSQL (MongoDB): flexible documents, no fixed schema, horizontal scaling, better for hierarchical or variable data. NoSQL trades ACID guarantees for flexibility and scale."},
        ],
        "medium": [
            {"q": "How do you perform a JOIN-equivalent operation in MongoDB?",
             "a": "Using $lookup in an aggregation pipeline: { $lookup: { from: 'subjects', localField: 'subjectId', foreignField: '_id', as: 'subjectInfo' } }. It's less efficient than SQL JOINs — MongoDB is designed for denormalized data where related info is embedded in one document."},
            {"q": "When should you embed documents vs. reference them in MongoDB?",
             "a": "Embed when data is always accessed together, has 1:1 or 1:few relationship, and doesn't change independently (e.g., student's address inside student doc). Reference when data is shared across documents, can grow unbounded, or is updated independently (e.g., subjects referenced by multiple students)."},
            {"q": "What is indexing in MongoDB and how does it improve query performance?",
             "a": "An index creates a sorted data structure on a field (B-tree), allowing MongoDB to find matching documents without scanning the entire collection. db.students.createIndex({ email: 1 }) makes login queries O(log n) instead of O(n). Without indexes, every query is a full collection scan."},
            {"q": "What are Mongoose schema validation and how does it help this project?",
             "a": "Mongoose lets you define field types, required fields, and custom validators: { marks: { type: Number, min: 0, max: 100, required: true } }. When saving, Mongoose validates the document and throws an error before hitting the database, preventing corrupt data."},
            {"q": "What is the aggregation pipeline in MongoDB?",
             "a": "A sequence of stages transforming documents: $match (filter), $group (aggregate — sum, avg), $sort, $project (reshape), $lookup (join). Example for average marks: [{ $group: { _id: '$studentId', avg: { $avg: '$marks' } } }]."},
        ],
        "hard": [
            {"q": "Explain MongoDB's consistency model and when you might prefer PostgreSQL instead.",
             "a": "MongoDB offers eventual consistency by default in replica sets — writes propagate asynchronously. For financial transactions or systems requiring strict ACID guarantees (bank transfers, inventory), PostgreSQL's serializable transactions are safer. MongoDB 4.0+ supports multi-document ACID transactions but with performance cost."},
            {"q": "How would you design the schema for this student result portal — embedded or referenced?",
             "a": "Student collection: { _id, name, email, role }. Results embedded if only accessed by one student: { _id, studentId (ref), subject, marks, grade }. Since admin needs to query results across all students, a separate Results collection referenced by studentId scales better for reporting queries."},
            {"q": "What is a MongoDB replica set and why would you use it in production?",
             "a": "A group of MongoDB servers maintaining the same dataset — one primary (handles writes) and multiple secondaries (replicate from primary). Provides high availability: if the primary fails, an election promotes a secondary automatically. Also enables read scaling by routing read queries to secondaries."},
        ],
    },

    # ─── SMART IRRIGATION ──────────────────────────────────────────────────────

    "mqtt": {
        "easy": [
            {"q": "What does MQTT stand for?",
             "a": "Message Queuing Telemetry Transport — a lightweight publish-subscribe messaging protocol designed for constrained devices and low-bandwidth networks."},
            {"q": "What is the role of the MQTT broker in this project?",
             "a": "The broker (Mosquitto) is the central server that receives messages from publishers and routes them to all subscribers on matching topics. Devices don't communicate directly — everything goes through the broker."},
            {"q": "What is a topic in MQTT? Give an example from this project.",
             "a": "A topic is a string used to route messages, organized in a hierarchy with / separators. In this project: 'farm/moisture' (NodeMCU publishes soil readings) and 'farm/pump' (Python subscriber publishes ON/OFF commands)."},
            {"q": "What is the difference between MQTT and HTTP for IoT devices?",
             "a": "HTTP is request-response and heavyweight — each request needs headers, connection setup. MQTT is publish-subscribe, lightweight, persistent connection, low overhead. A soil sensor on 3.3V battery cannot afford HTTP overhead but can easily run MQTT."},
            {"q": "What are the three MQTT Quality of Service (QoS) levels?",
             "a": "QoS 0: At most once (fire and forget — message may be lost). QoS 1: At least once (guaranteed delivery, may duplicate). QoS 2: Exactly once (guaranteed, no duplicate — slowest). For soil readings, QoS 0 is fine; for pump commands, QoS 1 or 2 is safer."},
            {"q": "What is Mosquitto?",
             "a": "Mosquitto is an open-source MQTT broker by Eclipse Foundation. It's lightweight, runs on Linux/Windows/Raspberry Pi, and is the most commonly used broker for IoT development and small deployments."},
        ],
        "medium": [
            {"q": "Explain the publish-subscribe pattern in this irrigation project end-to-end.",
             "a": "NodeMCU reads soil moisture → publishes to 'farm/moisture' on the broker. Python subscriber is subscribed to 'farm/moisture' → receives reading → if moisture < 30%, publishes 'ON' to 'farm/pump'. NodeMCU is also subscribed to 'farm/pump' → receives 'ON' → triggers relay to start water pump."},
            {"q": "What happens if the MQTT broker goes offline? How would you handle it?",
             "a": "Publishers and subscribers lose connection. MQTT clients have reconnect logic — they retry connection at intervals. For critical commands like pump control, use QoS 1 or 2 with persistent sessions (cleanSession=false) so the broker queues messages for offline clients and delivers them when they reconnect."},
            {"q": "What is a Last Will Testament (LWT) in MQTT and how is it useful here?",
             "a": "LWT is a message the broker publishes on behalf of a client if it disconnects ungracefully. Configure: if NodeMCU disconnects unexpectedly, broker publishes 'offline' to 'farm/status'. The monitoring dashboard can then alert the operator that the sensor node is unreachable."},
            {"q": "What is the difference between cleanSession=true and cleanSession=false?",
             "a": "cleanSession=true: broker discards all subscriptions and queued messages when client disconnects. cleanSession=false: broker maintains subscriptions and queues QoS 1/2 messages for offline clients. For the pump controller that may reboot, cleanSession=false ensures it receives missed commands."},
            {"q": "How would you secure MQTT communication in a production deployment?",
             "a": "Enable TLS/SSL (MQTT over port 8883 instead of 1883), use client certificate authentication or username/password, restrict topic access with ACLs (each device can only publish/subscribe to its own topics), and run the broker behind a firewall."},
        ],
        "hard": [
            {"q": "Design the MQTT topic hierarchy for a farm with 10 plots, each with 3 sensors.",
             "a": "farm/{plot_id}/sensor/{sensor_type}: farm/plot1/moisture, farm/plot1/temperature, farm/plot2/moisture. Pump commands: farm/{plot_id}/pump. Status: farm/{plot_id}/status. Wildcards let you subscribe to all moisture sensors: farm/+/moisture, or all plot1 data: farm/plot1/#."},
            {"q": "What is MQTT over WebSockets and when would you use it?",
             "a": "MQTT over WebSockets (port 9001) allows web browsers to connect directly to the MQTT broker. Used for real-time dashboards — the React/Vue dashboard subscribes to sensor topics directly without a backend intermediary. Regular MQTT (port 1883/8883) is for device-to-broker communication."},
            {"q": "How would you handle message ordering and deduplication for pump control commands?",
             "a": "Use QoS 2 (exactly-once delivery) for pump commands to prevent duplicate activation. Add a message_id and timestamp in the payload — the subscriber checks if message_id was already processed (cache last N ids) to handle any edge-case duplicates. Add a debounce: don't re-activate pump within 30 seconds."},
        ],
    },

    "iot_sensors": {
        "easy": [
            {"q": "What does IoT stand for?",
             "a": "Internet of Things — a network of physical devices embedded with sensors, software, and connectivity to collect and exchange data over the internet."},
            {"q": "How does a soil moisture sensor work?",
             "a": "Resistive sensors measure electrical resistance between two probes in soil — wet soil conducts electricity better (lower resistance). The analog voltage output varies with moisture level. The NodeMCU's ADC (A0 pin) reads this as 0-1023 and we map it to 0-100%."},
            {"q": "What is an ADC and why is it needed for analog sensors?",
             "a": "Analog-to-Digital Converter — converts a continuous analog voltage (0-3.3V from sensors) to a digital number (0-1023 for 10-bit ADC). Microcontrollers process digital values, so ADC is the bridge between physical world and digital computation."},
            {"q": "What is a relay module and what does it do in this project?",
             "a": "A relay is an electrically operated switch. The NodeMCU (3.3V logic) sends a digital signal to the relay module, which controls a high-power circuit (12V pump) using an electromagnet. It isolates low-voltage microcontroller circuits from high-voltage actuators."},
            {"q": "What is the difference between digital and analog sensors?",
             "a": "Digital sensors output discrete values (0 or 1) — like a button or PIR motion sensor. Analog sensors output continuous voltage proportional to the measured quantity — like a soil moisture sensor, temperature (LM35), or light (LDR). Analog signals require ADC conversion."},
        ],
        "medium": [
            {"q": "Why is the NodeMCU ESP8266 used instead of Arduino Uno for this project?",
             "a": "NodeMCU has built-in WiFi (ESP8266 chip), allowing it to connect to the internet and communicate via MQTT without additional shields. Arduino Uno has no wireless capability. NodeMCU also runs at 80MHz with 4MB flash vs Uno's 16MHz and 32KB."},
            {"q": "What is PWM and could it be used to control pump speed in this project?",
             "a": "Pulse Width Modulation — rapidly switching a digital pin ON/OFF at varying duty cycles (percentage of ON time). A 50% duty cycle gives ~50% power. For a pump, PWM could control flow rate instead of binary ON/OFF, though the relay in this design only supports binary switching."},
            {"q": "What is sensor calibration and why is it needed for this moisture sensor?",
             "a": "The raw ADC value (0-1023) must be mapped to a meaningful moisture percentage. Calibration: measure the ADC reading with the sensor in dry air (map to 0%) and fully submerged in water (map to 100%). The map() function in Arduino does this linear interpolation."},
            {"q": "How would you add temperature monitoring to this irrigation system?",
             "a": "Add a DHT11 or DHT22 sensor connected to a GPIO pin. Read temperature and humidity using the DHT Arduino library. Publish to a new MQTT topic: farm/temperature. The Python backend can then implement logic like: skip irrigation if temperature < 15°C or if it's raining (humidity > 90%)."},
        ],
        "hard": [
            {"q": "What are the power consumption considerations for a battery-powered IoT node?",
             "a": "NodeMCU in active WiFi mode draws ~80mA — a 1000mAh battery lasts ~12 hours. For field deployment: use deep sleep mode (NodeMCU draws 20µA sleeping), wake every 5 min, take reading, publish via MQTT, sleep again. This extends battery life to weeks. WiFi connection takes 2-3 seconds per wake — the main power cost."},
            {"q": "What is edge computing in IoT and how could it improve this irrigation system?",
             "a": "Edge computing processes data on the device or local server rather than cloud. Instead of sending raw moisture readings to a central Python server, put decision logic on the NodeMCU itself: if moisture < 30%, activate relay directly without MQTT round-trip. Reduces latency (from ~100ms to milliseconds), works without internet, and reduces bandwidth."},
        ],
    },

    "nodemcu_esp8266": {
        "easy": [
            {"q": "What is the ESP8266 and what makes it popular for IoT?",
             "a": "ESP8266 is a low-cost WiFi microchip with full TCP/IP stack made by Espressif. It has built-in WiFi, GPIO pins, ADC, and can be programmed with the Arduino IDE. Its ~$2-3 cost and WiFi capability make it the most popular IoT microcontroller."},
            {"q": "What IDE is used to program the NodeMCU in this project?",
             "a": "Arduino IDE — the NodeMCU board support package (ESP8266 by community) is added, allowing the same C++ Arduino syntax and library ecosystem to be used for NodeMCU programming."},
            {"q": "What is the difference between NodeMCU and ESP8266?",
             "a": "ESP8266 is the underlying chip/module (raw hardware). NodeMCU is a development board built around the ESP8266 that adds a USB-to-serial chip (CH340), power regulation, and breakout pins, making it easy to program and prototype."},
            {"q": "What does `delay(5000)` do in the NodeMCU firmware in this project?",
             "a": "It pauses execution for 5000 milliseconds (5 seconds) between moisture sensor readings. This creates a 5-second polling interval — the sensor publishes a new reading to MQTT every 5 seconds."},
        ],
        "medium": [
            {"q": "What is the PubSubClient library and why is it used in this project?",
             "a": "PubSubClient is an Arduino MQTT client library. It provides connect(), publish(), subscribe(), and callback() functions for MQTT communication. In this project it handles connecting to Mosquitto, publishing moisture readings, and receiving pump commands."},
            {"q": "What happens in the `callback` function in the NodeMCU firmware?",
             "a": "The callback is triggered when the broker delivers a subscribed message to the device. In this project, it reads the payload (ON or OFF), and sets the relay pin HIGH (activate pump) or LOW (deactivate pump) accordingly."},
            {"q": "What is the purpose of `client.loop()` in the NodeMCU main loop?",
             "a": "client.loop() processes incoming MQTT messages and maintains the connection to the broker. Without calling it regularly, the device won't receive subscribed messages and the connection may time out. It must be called frequently in the main loop."},
        ],
        "hard": [
            {"q": "Why does NodeMCU only have one analog input pin (A0) and what is its voltage range?",
             "a": "The ESP8266 chip has a single ADC channel with a 0-1V input range. NodeMCU adds a voltage divider on the board to extend it to 0-3.3V. If you need more analog inputs, you'd use an external multiplexer (CD4051) or an I2C ADC expander (ADS1115)."},
            {"q": "What is OTA (Over-The-Air) update and why is it important for deployed IoT devices?",
             "a": "OTA allows firmware updates over WiFi without physical access. Critical for deployed field devices — you can't bring a farmer's irrigation controller to a laptop to flash firmware. Arduino IDE and PlatformIO support OTA for ESP8266. Always include OTA support in production IoT deployments."},
        ],
    },

    # ─── HOUSE PRICE PREDICTION ────────────────────────────────────────────────

    "linear_regression": {
        "easy": [
            {"q": "What is linear regression?",
             "a": "A supervised machine learning algorithm that models the relationship between input features and a continuous target variable as a linear equation: y = w₁x₁ + w₂x₂ + ... + b. It finds the best-fit line that minimizes prediction error."},
            {"q": "What is the difference between simple and multiple linear regression?",
             "a": "Simple: one input feature (house size → price). Multiple: multiple features (size, bedrooms, location → price). This project uses multiple regression with all California housing features."},
            {"q": "What does the coefficient (weight) of a feature represent in linear regression?",
             "a": "The change in the target variable for a one-unit increase in that feature, holding all others constant. E.g., if the weight for 'rooms' is 5000, each additional room adds $5,000 to predicted house price."},
            {"q": "What is the mean squared error (MSE) loss function in linear regression?",
             "a": "MSE = average of squared differences between predicted and actual values: MSE = (1/n) Σ(y_pred - y_actual)². Squaring penalizes large errors more than small ones and makes the loss differentiable for gradient descent."},
            {"q": "What is gradient descent in the context of linear regression?",
             "a": "An optimization algorithm that iteratively adjusts weights in the direction that reduces the loss. Each step: w = w - learning_rate * ∂(Loss)/∂w. Continues until convergence (loss stops decreasing significantly)."},
        ],
        "medium": [
            {"q": "What is Ridge regression and how does it differ from ordinary linear regression?",
             "a": "Ridge adds L2 regularization to the loss: Loss = MSE + α * Σ(w²). The α term penalizes large weights, shrinking them toward zero. This prevents overfitting when features are correlated (multicollinearity) or when there are many features relative to samples."},
            {"q": "What is the difference between R² and RMSE as evaluation metrics?",
             "a": "R² (0 to 1): proportion of variance in target explained by the model. Higher is better. RMSE (root of MSE): average prediction error in the same units as target (dollars for house prices). Lower is better. R²=0.85 means the model explains 85% of price variance; RMSE=$50,000 means predictions are off by $50K on average."},
            {"q": "Why do we standardize features before training linear regression?",
             "a": "Linear regression uses gradient descent — features with large values (population: 100,000) have much larger gradients than small features (rooms: 5). Gradient descent converges slowly or diverges without scaling. StandardScaler normalizes each feature to mean=0, std=1, making gradient updates balanced."},
            {"q": "What is the difference between Lasso (L1) and Ridge (L2) regularization?",
             "a": "Lasso (L1): penalizes Σ|w|, can drive coefficients exactly to zero — performs feature selection. Ridge (L2): penalizes Σw², shrinks all coefficients but rarely zeros them. Use Lasso when you suspect many irrelevant features; Ridge when all features are relevant but correlated."},
            {"q": "What is underfitting vs overfitting in the context of this house price model?",
             "a": "Underfitting: model too simple (e.g., only using one feature) — high train error AND high test error. Overfitting: model memorizes training data (e.g., very high-degree polynomial) — low train error but high test error. Linear regression on reasonable features typically underfits; trees are more prone to overfitting."},
        ],
        "hard": [
            {"q": "What is the Ordinary Least Squares (OLS) closed-form solution for linear regression and when would you prefer gradient descent?",
             "a": "OLS: w = (XᵀX)⁻¹Xᵀy — finds optimal weights directly without iteration. Exact solution, no hyperparameters. However, inverting XᵀX is O(n³) in features and requires features < samples. For large feature sets (>10K), gradient descent scales better. For tabular data with <1000 features, OLS/scikit-learn is fine."},
            {"q": "Explain multicollinearity and how you would detect and handle it in this dataset.",
             "a": "Multicollinearity: two or more features are highly correlated (e.g., 'rooms' and 'bedrooms'). It makes individual coefficients unstable and uninterpretable — small data changes cause large coefficient swings. Detect: Variance Inflation Factor (VIF > 10 is concerning), correlation heatmap. Fix: drop one correlated feature, use Ridge regression, or apply PCA."},
            {"q": "Why does Random Forest outperform linear regression on the California housing dataset?",
             "a": "House prices have non-linear relationships (price/m² increases non-linearly with location quality) and feature interactions (oceanfront + large house > their individual effects summed). Linear regression can't capture these without manual feature engineering. Random Forest learns non-linear splits and interactions automatically."},
        ],
    },

    "cross_validation": {
        "easy": [
            {"q": "What is cross-validation and why is it used?",
             "a": "A technique to evaluate model performance more reliably by testing it on multiple data subsets. Instead of one train/test split, it trains and evaluates the model k times, each time using a different portion as the test set. Gives a more stable performance estimate."},
            {"q": "What is k-fold cross-validation? What is k typically set to?",
             "a": "The data is split into k equal folds. The model trains on k-1 folds and tests on the remaining 1 fold, rotating k times. Performance is averaged across all k runs. Typically k=5 or k=10."},
            {"q": "What is the advantage of cross-validation over a single train-test split?",
             "a": "A single split's result depends on which samples happened to land in train vs test — high variance. Cross-validation averages over k splits, giving a more reliable estimate and standard deviation of model performance."},
            {"q": "What does `cross_val_score(model, X, y, cv=5)` return?",
             "a": "An array of 5 scores — one per fold. For regression, typically negative MSE by default (sklearn convention). For classification, accuracy. Average these scores for the final performance estimate."},
        ],
        "medium": [
            {"q": "What is the difference between k-fold and stratified k-fold cross-validation?",
             "a": "Stratified k-fold ensures each fold has the same class distribution as the overall dataset. Important for imbalanced classification — without stratification, some folds might have very few samples of the minority class. For regression tasks, standard k-fold is used."},
            {"q": "What is the bias-variance tradeoff and how does cross-validation help assess it?",
             "a": "High bias: model too simple, underfits (high error on both train and test). High variance: model too complex, overfits (low train error, high test error). Cross-validation measures test error across multiple splits — high variance shows as large std deviation across fold scores."},
            {"q": "What is nested cross-validation and when is it needed?",
             "a": "When both selecting hyperparameters AND evaluating model performance on the same dataset, inner CV selects hyperparameters (GridSearchCV), outer CV evaluates the selected model on unseen data. Without nesting, hyperparameter selection on the same CV loop as evaluation causes optimistic bias."},
        ],
        "hard": [
            {"q": "Why does cross-validation still overestimate performance if feature selection is done before it?",
             "a": "Data leakage: if you select the best features using the full dataset (including test folds), those features are already tuned to the test data. The model then looks artificially better. Feature selection must be inside the cross-validation loop — done only on the training folds."},
            {"q": "What is time-series cross-validation and why can't you use standard k-fold for stock or weather data?",
             "a": "Standard k-fold randomly shuffles data — future data could end up in the training fold, creating leakage. Time-series CV (walk-forward validation): always train on past data, test on future. sklearn's TimeSeriesSplit implements this, increasing training set size with each fold while always keeping time order."},
        ],
    },

    # ─── NETWORK INTRUSION DETECTION ───────────────────────────────────────────

    "xgboost": {
        "easy": [
            {"q": "What does XGBoost stand for?",
             "a": "Extreme Gradient Boosting — an optimized implementation of gradient boosting that is fast, regularized, and handles missing values natively."},
            {"q": "What type of algorithm is XGBoost?",
             "a": "An ensemble learning algorithm — it builds many decision trees sequentially, where each tree corrects the errors of the previous trees. The final prediction is the sum of all tree predictions."},
            {"q": "What is boosting in machine learning?",
             "a": "An ensemble technique where models are trained sequentially — each new model focuses on the mistakes of the previous one. Unlike bagging (Random Forest, parallel), boosting is sequential and adaptive."},
            {"q": "Why does XGBoost often outperform Random Forest on tabular data?",
             "a": "XGBoost uses gradient boosting (sequential error correction), regularization (L1/L2), and second-order gradient information for more precise leaf value calculation. Random Forest trains trees independently and averages — XGBoost's sequential correction makes it more powerful for structured data."},
            {"q": "What is the n_estimators parameter in XGBoost?",
             "a": "The number of trees to build. More trees = more iterations of error correction. Too few → underfit. Too many → overfit (though XGBoost's regularization helps). Typically 100-500 for tabular data."},
        ],
        "medium": [
            {"q": "What is the difference between gradient boosting and gradient descent?",
             "a": "Gradient descent optimizes weights of a single model by moving in the direction of steepest loss decrease. Gradient boosting fits new models to the residuals (gradients of loss) of the current ensemble — each new tree is a 'step' in function space rather than weight space."},
            {"q": "What does the max_depth parameter control and what is a good value for network intrusion detection?",
             "a": "max_depth limits how deep each tree can grow — controls model complexity. Deeper trees = more complex patterns captured but risk overfitting. For NSL-KDD with 41 features, max_depth=4-6 typically balances accuracy and generalization. This project uses max_depth=6."},
            {"q": "What is the learning_rate (eta) in XGBoost and how does it interact with n_estimators?",
             "a": "Learning rate scales the contribution of each tree: final prediction += learning_rate * tree_prediction. Lower rate needs more trees but generalizes better. Common pattern: learning_rate=0.1 with n_estimators=200, or learning_rate=0.01 with n_estimators=2000. They tradeoff training time for accuracy."},
            {"q": "How does XGBoost handle missing values natively?",
             "a": "During training, XGBoost learns the optimal default direction (left or right branch) for missing values at each split. At inference, missing features go the learned direction. This is why XGBoost handles real-world messy data without imputation."},
            {"q": "What is the difference between classification_report and confusion_matrix?",
             "a": "Confusion matrix: table of TP, FP, TN, FN per class — shows raw count breakdown. Classification report: per-class precision, recall, F1-score, and support derived from the matrix. More readable summary — critical for imbalanced datasets like intrusion detection where class counts differ."},
        ],
        "hard": [
            {"q": "Explain the second-order gradient used in XGBoost and why it makes leaf value calculation more precise.",
             "a": "XGBoost uses both first-order (gradient, g) and second-order (Hessian, h) derivatives of the loss. Optimal leaf value = -Σg / (Σh + λ). The Hessian H provides curvature information — it tells how fast the gradient is changing, enabling a more precise Newton-Raphson step rather than gradient descent's constant step."},
            {"q": "What is early stopping in XGBoost and how does it prevent overfitting?",
             "a": "early_stopping_rounds=10: training stops if the validation metric doesn't improve for 10 consecutive rounds. Monitors eval_set performance — finds the optimal n_estimators without running all rounds. Prevents overfitting by stopping before test error starts rising, saving compute time."},
            {"q": "How would you handle class imbalance in the NSL-KDD dataset where normal traffic far outnumbers attacks?",
             "a": "XGBoost: set scale_pos_weight = (negative samples) / (positive samples) to weight the minority class. Alternatively, use SMOTE to oversample attack samples before training, or set class_weight='balanced' equivalent. Also: optimize for F1 or AUC-ROC rather than accuracy, which is misleading on imbalanced data."},
        ],
    },

    "network_security": {
        "easy": [
            {"q": "What is the difference between IDS and IPS?",
             "a": "IDS (Intrusion Detection System) monitors traffic and generates alerts but takes no action. IPS (Intrusion Prevention System) actively blocks or drops suspicious traffic in real time. This project implements an IDS."},
            {"q": "What are the four attack categories in the NSL-KDD dataset?",
             "a": "DoS (Denial of Service) — overwhelm resources. Probe — reconnaissance/port scanning. R2L (Remote to Local) — unauthorized remote access. U2R (User to Root) — privilege escalation from regular user to root."},
            {"q": "What is a port scan and why is it a security concern?",
             "a": "Systematically probing a host's ports to identify open services. Attackers use it for reconnaissance — finding which services are running to plan an exploit. Classified as a 'Probe' attack in NSL-KDD."},
            {"q": "What is a DoS attack and how does it affect network services?",
             "a": "Denial of Service — flooding a server with so many requests that it can't respond to legitimate users. The server runs out of CPU, memory, or bandwidth. Distributed DoS (DDoS) uses many machines simultaneously."},
        ],
        "medium": [
            {"q": "What is the false positive rate and why does it matter critically for an IDS?",
             "a": "False positive rate = legitimate traffic flagged as attacks / total legitimate traffic. High false positives cause alert fatigue — security analysts are overwhelmed by false alarms and start ignoring alerts, missing real threats. A useful IDS needs low FPR even at the cost of some true positive rate."},
            {"q": "Why is accuracy a misleading metric for intrusion detection on imbalanced data?",
             "a": "If 95% of traffic is normal, a model that classifies everything as normal achieves 95% accuracy while detecting zero attacks. Better metrics: precision, recall, F1-score per class, and AUC-ROC. For security, recall (catch rate of attacks) is more critical than precision."},
            {"q": "What is Scapy and how would you use it for real-time packet analysis?",
             "a": "Scapy is a Python library for network packet manipulation. sniff(iface='eth0', prn=packet_handler) captures live packets. For each packet, extract features (duration, protocol, src/dst bytes) and run the trained XGBoost model to classify it as normal or attack type."},
            {"q": "What is feature extraction from raw network packets and why is it necessary?",
             "a": "ML models can't directly process raw packet bytes. Feature extraction derives meaningful numeric attributes: duration, protocol_type, src_bytes, dst_bytes, flag, error rates, connection counts. The NSL-KDD dataset has 41 such pre-extracted features. For live traffic, Scapy reads packets and computes these features."},
        ],
        "hard": [
            {"q": "What is the difference between signature-based and anomaly-based intrusion detection? Which is this project?",
             "a": "Signature-based: matches traffic against known attack patterns (like antivirus). Fast, low FP, but can't detect novel attacks. Anomaly-based: learns what 'normal' looks like and flags deviations. Can detect zero-day attacks but higher FPR. This project uses ML-based anomaly/classification — trained on labeled examples (hybrid)."},
            {"q": "What is adversarial machine learning and how does it threaten this NIDS?",
             "a": "Attackers who know the model can craft adversarial examples — slightly modify attack packets (slow their rate, fragment packets) to evade detection while still achieving the attack goal. Defense: ensemble models (harder to fool all at once), adversarial training, and behavioral monitoring over time rather than per-packet classification."},
        ],
    },

    # ─── FACE RECOGNITION ATTENDANCE ───────────────────────────────────────────

    "face_recognition_dlib": {
        "easy": [
            {"q": "What library is used for face recognition in this project?",
             "a": "The face_recognition library — a Python wrapper around dlib's face recognition model, which uses deep learning to generate 128-dimensional face encodings."},
            {"q": "What is a face encoding?",
             "a": "A 128-dimensional numerical vector representing a face, generated by a deep neural network (ResNet-based). Two photos of the same person produce similar vectors; different people produce distant vectors."},
            {"q": "How is face matching performed in this project?",
             "a": "Compare the Euclidean distance between two face encodings. If the distance is below a threshold (typically 0.6), the faces are considered a match. face_recognition.compare_faces() does this comparison."},
            {"q": "What does `face_recognition.face_locations()` return?",
             "a": "A list of tuples (top, right, bottom, left) — the pixel coordinates of each detected face in the image. These bounding box coordinates are used to draw rectangles around faces."},
            {"q": "Why do we resize the frame to 25% size before face detection in this project?",
             "a": "Face detection on full HD frames (1920×1080) is slow. Scaling to 25% (480×270) speeds up detection ~16x. Detected face coordinates are then scaled back up (×4) to draw rectangles on the original full-resolution frame."},
        ],
        "medium": [
            {"q": "What is the 128-dimensional embedding space and why 128 dimensions specifically?",
             "a": "The dlib model projects each face into a 128-D space where the same person's faces cluster together across different lighting/angles/expressions. 128 was chosen empirically as a good balance between discriminative power and computation cost. The model was trained with triplet loss to maximize inter-person distance and minimize intra-person distance."},
            {"q": "What is the tolerance parameter and how does changing it affect the system?",
             "a": "Tolerance is the Euclidean distance threshold for a match. Default 0.6. Lower (0.4) = stricter — fewer false positives but more false negatives (missing known students). Higher (0.7) = looser — more true positives but risk of misidentifying strangers as registered students. Calibrate based on your false positive/negative tolerance."},
            {"q": "Why is BGR to RGB conversion needed before processing with face_recognition?",
             "a": "OpenCV reads images in BGR (Blue-Green-Red) channel order by convention. The face_recognition library (and most deep learning frameworks) expect RGB. Without conversion, colors are wrong and face detection/encoding quality degrades significantly."},
            {"q": "How is attendance marked to prevent duplicate entries in this project?",
             "a": "Before inserting, the SQLite database is queried: SELECT id WHERE name=? AND date=today. If a row already exists, no insert is made. This ensures each student is marked present only once per day regardless of how many times their face is recognized."},
            {"q": "What are the limitations of this face recognition system in real-world conditions?",
             "a": "Performance degrades with: poor/uneven lighting, extreme face angles (profile view), low webcam resolution, face masks/glasses, and twins. The system can also be spoofed by holding a photo in front of the camera."},
        ],
        "hard": [
            {"q": "What is triplet loss and how is it used to train dlib's face encoding model?",
             "a": "Triplet loss trains the network using three images: anchor (person A), positive (another photo of A), negative (person B). Loss = max(0, dist(anchor, positive) - dist(anchor, negative) + margin). The network learns to minimize distance between same-person pairs and maximize distance between different-person pairs, creating the discriminative 128-D embedding space."},
            {"q": "How would you implement liveness detection to prevent photo spoofing?",
             "a": "Methods: (1) Blink detection — track eye aspect ratio using facial landmarks (dlib 68-point model) and require at least one blink. (2) Challenge-response — randomly request head turn left/right and verify. (3) 3D depth sensors (like FaceID's structured light). (4) Texture analysis — real skin has different micro-texture than printed photos. Libraries: imutils for landmark tracking."},
            {"q": "How would you scale this system to handle 1000+ students efficiently?",
             "a": "Current approach: compare every frame's face against all stored encodings — O(N) per frame. At 1000 students at 30fps, this becomes slow. Solutions: (1) Use FAISS (Facebook AI Similarity Search) for approximate nearest neighbor search — O(log N). (2) Pre-cluster students by class/section, only compare against the expected group. (3) GPU acceleration for batch encoding computation."},
        ],
    },

    "opencv": {
        "easy": [
            {"q": "What is OpenCV and what does it stand for?",
             "a": "Open Source Computer Vision Library — a library of programming functions for real-time computer vision tasks like image processing, video capture, object detection, and image transformation."},
            {"q": "What does `cv2.VideoCapture(0)` do?",
             "a": "Opens the default webcam (device 0). VideoCapture can also open video files by passing a file path. Returns a VideoCapture object used to read frames."},
            {"q": "What does `cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)` do?",
             "a": "Converts a frame from BGR color space (OpenCV default) to RGB (used by face_recognition and PIL). The channels are reordered from [B,G,R] to [R,G,B]."},
            {"q": "What does `cv2.rectangle()` do in this project?",
             "a": "Draws a colored rectangle on the frame around a detected face: cv2.rectangle(frame, (left, top), (right, bottom), color, thickness). Green for recognized faces, red for unknown."},
            {"q": "What does `cv2.waitKey(1) & 0xFF == ord('q')` do?",
             "a": "Waits 1ms for a keypress. If the 'q' key is pressed, the expression is True and the while loop breaks, stopping the webcam stream. `& 0xFF` masks to the lower 8 bits for compatibility across platforms."},
        ],
        "medium": [
            {"q": "Why do we use `cv2.resize(frame, (0, 0), fx=0.25, fy=0.25)` instead of specifying a fixed size?",
             "a": "Using scale factors (fx, fy) scales relative to the current frame size. If the webcam resolution changes (720p vs 1080p), the 25% scale still applies correctly. A fixed pixel size would distort the aspect ratio for different resolutions."},
            {"q": "What is the difference between `ret, frame = cap.read()` and what does `ret` indicate?",
             "a": "`ret` is a boolean — True if the frame was successfully read, False if the camera disconnected or the video ended. Always check `ret` before processing `frame` to avoid errors when the stream fails."},
            {"q": "How would you save recognized faces' images to disk for logging?",
             "a": "cv2.imwrite(f'logs/{name}_{datetime.now()}.jpg', frame[top:bottom, left:right]) — saves the cropped face region. Useful for audit logging. Add a timestamp to the filename to avoid overwriting."},
            {"q": "What is frame rate (FPS) and how would you display it on the video feed?",
             "a": "FPS = frames processed per second. Calculate: fps = 1 / (time.time() - prev_time). Display: cv2.putText(frame, f'FPS: {fps:.1f}', (10, 30), cv2.FONT_HERSHEY_SIMPLEX, 1, (0,255,0), 2). Typical webcam: 30 FPS. With face recognition, may drop to 5-10 FPS without optimization."},
        ],
        "hard": [
            {"q": "What is the difference between Haar cascades and deep learning-based face detection? Which does dlib use?",
             "a": "Haar cascades (OpenCV's cv2.CascadeClassifier) use handcrafted features — fast but inaccurate at extreme angles. dlib uses HOG+SVM for detection (moderate), or a CNN-based face detector (accurate, slower). face_recognition.face_locations() uses HOG by default; model='cnn' for CNN-based detection. Deep learning detectors (RetinaFace, MTCNN) are most accurate."},
            {"q": "How would you implement real-time attendance with multi-threading to maintain 30 FPS?",
             "a": "Video capture and face recognition are on different threads. Main thread captures frames at 30 FPS and displays. Worker thread performs face recognition (slow, 100-500ms) on every Nth frame. Use a thread-safe queue: main puts frames, worker gets them. Only the latest result from the worker is displayed. Python's threading + queue module implements this."},
        ],
    },

    "flask_web": {
        "easy": [
            {"q": "What is Flask and how does it differ from Django?",
             "a": "Flask is a lightweight Python web microframework — minimal built-ins, high flexibility. Django is a full-stack framework — includes ORM, admin panel, authentication, templating. Flask is better for APIs and small apps; Django for large, feature-rich web applications."},
            {"q": "What does `@app.route('/attendance', methods=['GET'])` do in Flask?",
             "a": "Registers a URL route: when a GET request hits /attendance, Flask calls the decorated function. The response is returned to the client (typically HTML or JSON)."},
            {"q": "What does `app.run(debug=True)` do?",
             "a": "Starts the Flask development server. debug=True enables: auto-reload on code changes, detailed error pages with traceback in the browser. Never use debug=True in production — it exposes the Werkzeug debugger."},
            {"q": "How do you return JSON from a Flask endpoint?",
             "a": "Use Flask's jsonify(): return jsonify({'students': data, 'count': len(data)}), 200. This sets the Content-Type header to application/json and converts the Python dict to JSON."},
        ],
        "medium": [
            {"q": "What is Jinja2 and how is it used in the Flask attendance dashboard?",
             "a": "Jinja2 is Flask's built-in templating engine. Templates (HTML files) use {{ variable }} for output and {% for %}/{% if %} for logic. Flask's render_template('attendance.html', data=rows) passes Python data to the template for rendering the attendance table."},
            {"q": "What is the difference between `request.form` and `request.json` in Flask?",
             "a": "request.form: parses form data (application/x-www-form-urlencoded, multipart/form-data) — from HTML forms. request.json: parses JSON body (application/json) — from fetch/axios API calls. Use request.get_json() as the safe alternative to request.json."},
            {"q": "How would you add login protection to the Flask admin dashboard?",
             "a": "Use Flask-Login extension: decorate protected routes with @login_required. Store admin credentials (hashed with bcrypt) in the database. On /login POST, verify credentials, create a user session. Flask-Login manages session cookies and redirects unauthorized users to the login page."},
        ],
        "hard": [
            {"q": "What is WSGI and why can't Flask's built-in server be used in production?",
             "a": "WSGI (Web Server Gateway Interface) is the Python standard for web servers and apps. Flask's built-in server is single-threaded — one request at a time. In production, use Gunicorn (multi-process WSGI server): gunicorn -w 4 app:app. Behind Nginx as a reverse proxy for SSL termination and static file serving."},
            {"q": "How would you make the face recognition results available to the Flask dashboard in real time?",
             "a": "Option 1: WebSockets (Flask-SocketIO) — push attendance events to the browser instantly. Option 2: Server-Sent Events — one-way push from server to browser. Option 3: Frontend polls /api/attendance every 5 seconds (simplest, slight delay). For a classroom dashboard, polling every 5s is acceptable."},
        ],
    },

    # ─── LIBRARY MANAGEMENT ────────────────────────────────────────────────────

    "spring_boot": {
        "easy": [
            {"q": "What is Spring Boot and how does it differ from the Spring Framework?",
             "a": "Spring Boot is an opinionated extension of the Spring Framework that provides auto-configuration, embedded servers (Tomcat), and starter dependencies — eliminating boilerplate XML configuration. You get a production-ready app faster."},
            {"q": "What does @SpringBootApplication annotation do?",
             "a": "It combines three annotations: @EnableAutoConfiguration (auto-configure based on classpath), @ComponentScan (scan for Spring components in the package), and @Configuration (allow bean definitions). It's the entry point annotation for a Spring Boot application."},
            {"q": "What is an embedded server in Spring Boot and what is the default?",
             "a": "Spring Boot embeds a web server inside the JAR file — you run java -jar app.jar without installing Tomcat separately. The default embedded server is Apache Tomcat (port 8080). You can switch to Jetty or Undertow in pom.xml."},
            {"q": "What does `mvn spring-boot:run` do?",
             "a": "Maven command that compiles the Spring Boot project and starts the embedded Tomcat server in development mode. Equivalent to building the JAR and running it, but with faster iteration."},
            {"q": "What is the application.properties file in Spring Boot?",
             "a": "The main configuration file for a Spring Boot application. Configure: server port, database URL/credentials, logging levels, JPA settings, etc. Spring Boot reads this file automatically at startup. Can also use application.yml for YAML format."},
        ],
        "medium": [
            {"q": "What is the @Transactional annotation and why is it critical in the library management system?",
             "a": "@Transactional wraps a method in a database transaction — all operations succeed or all are rolled back. Critical in issueBook(): if book copies decrement succeeds but issuance record insert fails, @Transactional rolls back both. Without it, data becomes inconsistent."},
            {"q": "What is the difference between @RestController and @Controller in Spring Boot?",
             "a": "@Controller returns views (HTML via Thymeleaf) — used in server-side rendering. @RestController = @Controller + @ResponseBody — every method returns JSON/data directly to the HTTP response. For REST APIs, use @RestController."},
            {"q": "What does @Autowired do in Spring Boot?",
             "a": "It injects a dependency (Spring-managed bean) automatically by type. Instead of manually creating new BookRepository(), Spring finds the matching bean in its context and injects it. This is Dependency Injection (DI) — decouples code, makes testing easier."},
            {"q": "What is the difference between @Service, @Repository, and @Controller stereotypes?",
             "a": "All three are specializations of @Component (Spring-managed bean). @Controller: handles HTTP requests. @Service: business logic layer. @Repository: data access layer — also enables exception translation (DataAccessException from JPA exceptions). Using the right one documents intent and enables layer-specific features."},
            {"q": "What is Spring Security and how would you add basic auth to this project?",
             "a": "Spring Security is an authentication/authorization framework. Add spring-boot-starter-security to pom.xml. Auto-configures HTTP basic auth with a generated password. Customize: extend WebSecurityConfigurerAdapter, define roles (LIBRARIAN, MEMBER), protect routes with .antMatchers('/admin/**').hasRole('LIBRARIAN')."},
        ],
        "hard": [
            {"q": "What is Spring Bean lifecycle and what are the key phases?",
             "a": "1. Instantiation: Spring creates the bean object. 2. Dependency injection: @Autowired fields are populated. 3. @PostConstruct: initialization method runs. 4. Bean is ready for use. 5. @PreDestroy: cleanup before shutdown. Useful for: loading data at startup (@PostConstruct), closing connections at shutdown (@PreDestroy)."},
            {"q": "Explain the difference between @Transactional propagation levels: REQUIRED vs REQUIRES_NEW.",
             "a": "REQUIRED (default): joins existing transaction if one exists; creates new if none. REQUIRES_NEW: always creates a NEW transaction, suspending the existing one. Use REQUIRES_NEW for: audit logging (must persist even if main transaction rolls back), sending notifications (must record notification sent even if business logic fails)."},
            {"q": "What is Spring AOP and how could it be used in this library system?",
             "a": "Aspect-Oriented Programming — adds cross-cutting concerns (logging, security, timing) without modifying business code. Example: @Around advice on all @Service methods logs method name, arguments, execution time, and result. @AfterReturning on issueBook() sends an email notification. Keeps business code clean."},
        ],
    },

    "jpa_hibernate": {
        "easy": [
            {"q": "What is JPA and what is Hibernate?",
             "a": "JPA (Java Persistence API) is a specification — a set of interfaces for ORM in Java. Hibernate is the most popular implementation. JPA defines what to do; Hibernate defines how to do it (generates SQL, manages connections)."},
            {"q": "What does ORM stand for and what problem does it solve?",
             "a": "Object-Relational Mapping — bridges the gap between Java objects (OOP) and relational database tables (SQL). Without ORM, you write raw SQL for every CRUD operation. With ORM, you work with Java objects and Hibernate generates the SQL automatically."},
            {"q": "What does the @Entity annotation do?",
             "a": "Marks a Java class as a JPA entity — it will be mapped to a database table. The class must have an @Id field (primary key). Hibernate creates and manages the corresponding table."},
            {"q": "What does @GeneratedValue(strategy = GenerationType.IDENTITY) do?",
             "a": "Tells JPA to use the database's auto-increment feature for generating primary key values. MySQL's AUTO_INCREMENT, PostgreSQL's SERIAL. The database assigns the next available ID when a new record is inserted."},
            {"q": "What is a JpaRepository and what methods does it provide out of the box?",
             "a": "A Spring Data interface that provides CRUD operations without writing any SQL: save(), findById(), findAll(), deleteById(), existsById(), count(). Extend it: interface BookRepository extends JpaRepository<Book, Long> and you get all these methods automatically."},
        ],
        "medium": [
            {"q": "What is the difference between eager and lazy loading in JPA?",
             "a": "Eager: related entities are loaded immediately with the parent (one SQL join query). Lazy: related entities are loaded only when accessed (additional SQL query on demand). Default: @ManyToOne is eager, @OneToMany is lazy. Always use lazy loading for collections to avoid loading thousands of records unintentionally."},
            {"q": "What is the N+1 query problem and how do you fix it?",
             "a": "Loading N books triggers 1 query for books + N queries to load each book's issuances lazily = N+1 total queries. Fix: use JOIN FETCH in JPQL: 'SELECT b FROM Book b JOIN FETCH b.issuances' — one query loads everything. Or use @EntityGraph to specify eager loading per query without changing the mapping."},
            {"q": "What is the difference between @OneToMany and @ManyToOne and where does the foreign key go?",
             "a": "@OneToMany: on the 'one' side — one Member has many Issuances. @ManyToOne: on the 'many' side — many Issuances belong to one Member. The foreign key (member_id) always lives in the @ManyToOne side's table (issuances.member_id). @ManyToOne owns the relationship."},
            {"q": "What is Hibernate's first-level cache and how does it affect this library system?",
             "a": "First-level cache (session cache): within a single @Transactional session, the same entity is only fetched from DB once — subsequent accesses return the cached object. In issueBook(), if you load a Book then load it again in the same transaction, only one DB query fires. The cache is cleared when the transaction ends."},
            {"q": "What does schema auto-generation (`spring.jpa.hibernate.ddl-auto=update`) do?",
             "a": "Hibernate compares the entity model to the actual DB schema and alters the database to match (adds columns, creates tables). In development: use create-drop (create fresh on start). In production: NEVER use update or create-drop — use Flyway or Liquibase for versioned, safe schema migrations."},
        ],
        "hard": [
            {"q": "What are optimistic vs pessimistic locking and when would you use each in this library system?",
             "a": "Optimistic locking (@Version field): assumes no conflict, checks version on update — if someone else updated first, throws OptimisticLockException. Low overhead. Use for: updating student profiles (rare conflict). Pessimistic locking (SELECT FOR UPDATE): locks the row immediately. Use for: book copies (two librarians issuing the last copy simultaneously must serialize)."},
            {"q": "Explain the difference between EntityManager.persist(), merge(), and refresh().",
             "a": "persist(): makes a new transient entity managed and persistent (INSERT on flush). merge(): updates a detached entity — copies state to a managed entity (UPDATE on flush). refresh(): reloads managed entity from DB, discarding any unsaved changes. Important: JPA repositories' save() calls persist for new entities and merge for existing ones."},
        ],
    },

    "mysql_db": {
        "easy": [
            {"q": "What is MySQL and what type of database is it?",
             "a": "MySQL is an open-source relational database management system (RDBMS). Data is stored in tables with rows and columns, related through foreign keys. Uses SQL (Structured Query Language) for data manipulation."},
            {"q": "What is the difference between PRIMARY KEY and UNIQUE KEY?",
             "a": "PRIMARY KEY: uniquely identifies each row, cannot be NULL, only one per table. UNIQUE KEY: ensures all values in a column are unique, can be NULL (one NULL per column typically), multiple per table. Members.email would be UNIQUE; Members.id is PRIMARY KEY."},
            {"q": "What is a foreign key and why is it used in this library system?",
             "a": "A foreign key references the primary key of another table, enforcing referential integrity. issuances.book_id references books.id — you can't issue a book that doesn't exist. If you delete a book, MySQL prevents deletion if active issuances reference it (or cascades deletion)."},
            {"q": "What does INNER JOIN do? Give an example from this project.",
             "a": "Returns rows that have matching values in both tables. Example: SELECT m.name, b.title, i.due_date FROM issuances i INNER JOIN members m ON i.member_id = m.id INNER JOIN books b ON i.book_id = b.id — returns all issuances with member names and book titles."},
        ],
        "medium": [
            {"q": "What is the difference between INNER JOIN, LEFT JOIN, and RIGHT JOIN?",
             "a": "INNER JOIN: only rows with matches in BOTH tables. LEFT JOIN: all rows from left table + matching rows from right (NULL if no match) — useful for 'all members and their issuances (if any)'. RIGHT JOIN: all rows from right table + matching from left. FULL OUTER JOIN: all rows from both (not supported in MySQL, use UNION)."},
            {"q": "What is database normalization and which normal form is this library schema in?",
             "a": "Normalization eliminates data redundancy by organizing tables. 1NF: atomic values, no repeating groups. 2NF: no partial dependencies on composite key. 3NF: no transitive dependencies. The library schema (separate Books, Members, Issuances tables, no repeated data) is in 3NF."},
            {"q": "What is an index in MySQL and when would you add one in this system?",
             "a": "An index creates a sorted data structure (B-tree) on a column, making lookups O(log n) instead of O(n) full table scan. Add index on: issuances.member_id (frequently queried), books.isbn (search by ISBN), members.email (login lookup). Primary keys are automatically indexed."},
        ],
        "hard": [
            {"q": "What is a database transaction and what are the ACID properties?",
             "a": "A transaction is a unit of work that must succeed or fail completely. ACID: Atomicity (all or nothing), Consistency (DB moves from one valid state to another), Isolation (concurrent transactions don't interfere), Durability (committed data survives crashes). Critical for book issuance: decrement copies AND create issuance record must be atomic."},
            {"q": "What is the difference between InnoDB and MyISAM storage engines in MySQL?",
             "a": "InnoDB: supports transactions (ACID), foreign keys, row-level locking, crash recovery. MyISAM: no transactions, no foreign keys, table-level locking, faster for read-heavy. Always use InnoDB for production applications. MySQL 5.5+ defaults to InnoDB."},
        ],
    },

    "oop_java": {
        "easy": [
            {"q": "What are the four pillars of OOP?",
             "a": "Encapsulation (bundling data and methods, hiding internals), Inheritance (subclass inherits parent class properties), Polymorphism (same method name, different behavior), Abstraction (hiding implementation details behind interfaces/abstract classes)."},
            {"q": "What is the difference between an abstract class and an interface in Java?",
             "a": "Abstract class: can have method implementations, instance variables, constructors. A class can extend only one. Interface: only method signatures (Java 8+ allows default methods), constants. A class can implement multiple. Use interface for 'can do' (Serializable, Comparable); abstract class for 'is a' with shared implementation."},
            {"q": "What is encapsulation and how is it applied in the Book class?",
             "a": "Making fields private and providing public getters/setters to control access. private int availableCopies; public int getAvailableCopies(); public void setAvailableCopies(int n). This lets you add validation in the setter (e.g., copies >= 0) without changing all the code that uses it."},
            {"q": "What does the @Override annotation mean in Java?",
             "a": "It indicates the method overrides a method in the parent class or interface. Java compiler checks at compile time that the signature actually matches a parent method — catches typos. Without @Override, a slightly different signature would be a new method rather than an override, with no compile error."},
        ],
        "medium": [
            {"q": "Explain the SOLID principles with examples from the library management system.",
             "a": "S (Single Responsibility): BookService only handles book logic, MemberService handles members. O (Open/Closed): new media type (DVD) extends a base Media class without modifying existing code. L (Liskov): DVD and Book behave correctly wherever Media is expected. I (Interface Segregation): IssuableItem interface only for items that can be issued. D (Dependency Inversion): BookIssuanceService depends on IBookRepository interface, not concrete implementation."},
            {"q": "What is the difference between composition and inheritance? Which is preferred in modern OOP?",
             "a": "Inheritance: 'is-a' relationship, subclass inherits all parent methods (tight coupling). Composition: 'has-a' relationship, a class contains an instance of another class. Prefer composition — it's more flexible, avoids deep inheritance hierarchies, and allows changing behavior at runtime. Example: Book HAS-A Author (composition) rather than Book IS-AN Author (inheritance)."},
            {"q": "What is a Java generic and why is it used in JpaRepository<Book, Long>?",
             "a": "Generics parameterize types — write type-safe code without casting. JpaRepository<Book, Long> means: this repository manages Book entities with Long primary keys. Without generics: repository returns Object, requires casting (unsafe). With generics: repository returns Book directly (type-safe, compile-time checks)."},
        ],
        "hard": [
            {"q": "What is the Builder pattern and how would it benefit the Issuance object creation in this project?",
             "a": "Builder pattern separates object construction from its representation, useful for objects with many optional fields. Instead of a constructor with 8 parameters: Issuance.builder().book(book).member(member).issueDate(today).dueDate(today.plusDays(14)).status('ISSUED').build(). Lombok's @Builder annotation generates this automatically."},
            {"q": "Explain Java's Stream API and rewrite the fine calculation using streams.",
             "a": "Streams provide functional-style operations on collections. Original: loop through issuances, sum fines. Stream version: issuances.stream().filter(i -> i.getStatus().equals('RETURNED')).mapToDouble(Issuance::getFine).sum(). More readable, parallelizable with .parallelStream(), and composable with filter/map/reduce operations."},
        ],
    },

    # ─── EXPENSE TRACKER ───────────────────────────────────────────────────────

    "flutter_dart": {
        "easy": [
            {"q": "What is Flutter and what makes it different from React Native?",
             "a": "Flutter is Google's UI toolkit for building natively compiled apps from a single codebase (mobile, web, desktop). It uses Dart language and its own rendering engine (Skia/Impeller) — doesn't rely on native UI components. React Native uses JavaScript and bridges to native components. Flutter is known for smooth 60/120fps animations."},
            {"q": "What is the difference between StatefulWidget and StatelessWidget?",
             "a": "StatelessWidget is immutable — rebuilds only when its parent rebuilds. No internal state. StatefulWidget maintains internal state via a State object — calling setState() triggers a rebuild. Use Stateless for pure display; Stateful for interactive UI (forms, counters, lists that change)."},
            {"q": "What is Dart and is it similar to any other language?",
             "a": "Dart is Google's programming language — strongly typed, object-oriented, compiled to native code (AOT) or JavaScript. Syntax is similar to Java/C# with some JavaScript-like features. Hot reload in Flutter works because of Dart's VM during development."},
            {"q": "What is hot reload in Flutter?",
             "a": "A development feature that injects updated code into the running app in under a second without losing state. Press 'r' in the terminal. Changes to build() methods appear instantly. Hot restart (R) restarts the app from scratch — needed for main() changes."},
            {"q": "What does `flutter pub get` do?",
             "a": "Downloads all packages listed in pubspec.yaml from pub.dev (Flutter's package registry). Equivalent to npm install in JavaScript or pip install in Python. Must be run after cloning a project or adding new dependencies."},
        ],
        "medium": [
            {"q": "What is the widget tree in Flutter and how does rebuilding work?",
             "a": "Flutter UI is a tree of widgets — every UI element (Text, Button, Column, Container) is a widget. When setState() is called, Flutter rebuilds the subtree of the StatefulWidget that called it. Flutter diffs the old and new widget trees and only updates what changed in the render tree."},
            {"q": "What is the Provider package and how does it implement state management?",
             "a": "Provider uses InheritedWidget internally to share state down the widget tree without passing via constructors. ChangeNotifier holds state and calls notifyListeners() on change. Widgets wrap with Consumer<TransactionProvider> to rebuild when notified. Simpler than BLoC for medium-complexity apps."},
            {"q": "What is the difference between async/await and Future in Dart?",
             "a": "Future represents a value available later (like JavaScript Promise). async/await is syntactic sugar: async marks a function as asynchronous, await pauses execution until the Future completes. Without async/await: future.then((data) => ...). With: final data = await future; — more readable."},
            {"q": "What is pubspec.yaml and what goes in it?",
             "a": "Flutter's project manifest file — defines: app name, version, SDK constraints, dependencies (packages from pub.dev), dev_dependencies (testing tools), and assets (images, fonts to bundle). Similar to package.json in Node.js or requirements.txt in Python."},
        ],
        "hard": [
            {"q": "What is the difference between BLoC and Provider state management patterns?",
             "a": "BLoC (Business Logic Component): separates UI from business logic using streams — UI sends Events, BLoC processes them and emits States. More structured, better for complex flows, easier to test. Provider: simpler ChangeNotifier model, less boilerplate. BLoC for large apps with complex state; Provider for small-medium apps. Both are production-worthy."},
            {"q": "How does Flutter's rendering pipeline differ from React Native's bridge architecture?",
             "a": "React Native: JavaScript runs in a separate thread and communicates with native UI components via a bridge (serialized JSON). Asynchronous bridge can cause jank. Flutter: Dart compiles to native ARM code, Skia renders directly to the GPU with no bridge. Result: consistent 60fps, no bridge bottleneck, but can't use native components directly."},
        ],
    },

    "sqlite_mobile": {
        "easy": [
            {"q": "What is SQLite and why is it used in mobile apps?",
             "a": "SQLite is a lightweight embedded relational database stored as a single .db file. No server needed. Perfect for mobile apps — stores data locally on the device, works offline, fast for small-medium data. Used by WhatsApp, Facebook, and most Android/iOS apps for local storage."},
            {"q": "What Flutter package is used for SQLite in this project?",
             "a": "sqflite — Flutter's SQLite plugin. Provides asynchronous database operations (insert, query, update, delete) and runs on both Android and iOS."},
            {"q": "What is a database helper class and why is it implemented as a singleton?",
             "a": "A class that manages all database operations (open, create, CRUD). Singleton ensures only one database connection exists — prevents multiple connections to the same file causing locks or inconsistent state. Implemented with a private constructor and a static instance getter."},
            {"q": "What does `ON DELETE CASCADE` do in a SQLite foreign key?",
             "a": "When the parent record is deleted, all child records referencing it are automatically deleted. Example: deleting a category deletes all transactions in that category. Without CASCADE, SQLite would reject the parent deletion (foreign key constraint violation)."},
        ],
        "medium": [
            {"q": "Why must database operations be asynchronous in Flutter?",
             "a": "Database I/O is blocking — reading from disk takes milliseconds which freezes the UI if run on the main thread. Flutter's UI runs on the main isolate. Async/await with Future-returning sqflite methods runs I/O on a background thread, keeping the UI responsive."},
            {"q": "What is the difference between using SQLite locally vs Firebase Firestore?",
             "a": "SQLite: local, offline-first, private to device, no internet needed, not synced across devices. Firestore: cloud, real-time sync across devices, requires internet, has offline cache. Use SQLite for: personal finance (private data, offline). Use Firestore for: shared data (collaborative apps, chat)."},
            {"q": "How would you migrate the SQLite schema when you add a new column in a future app update?",
             "a": "Increment the database version in openDatabase(version: 2). Provide an onUpgrade callback: if (oldVersion < 2) { db.execute('ALTER TABLE transactions ADD COLUMN notes TEXT'); }. SQLite's ALTER TABLE only supports ADD COLUMN — for complex changes, create new table, migrate data, drop old."},
        ],
        "hard": [
            {"q": "How would you encrypt the SQLite database in this expense tracker for sensitive financial data?",
             "a": "Use sqflite_sqlcipher package — encrypts the entire database file with AES-256. At database open: openDatabase(path, password: 'user_provided_key'). The encryption key should be derived from the user's PIN using PBKDF2 (cryptography package), not stored in plain text. Consider Android Keystore for key storage."},
            {"q": "How would you implement database backup and restore in this Flutter app?",
             "a": "Backup: close the database, copy the .db file to the device's Documents directory or export to JSON using CRUD queries. Restore: copy the file back and reopen. For cloud backup: upload the .db file to Firebase Storage or Google Drive API. For cross-platform restore: export to JSON (since SQLite binary format may differ between platforms)."},
        ],
    },

    # ─── FEATURE ENGINEERING (house-price-prediction) ──────────────────────────

    "feature_engineering": {
        "easy": [
            {"q": "What is feature engineering?",
             "a": "The process of transforming raw data into features that better represent the underlying problem to the predictive model — creating new columns, scaling existing ones, encoding categories, or extracting info from text/dates."},
            {"q": "What is one-hot encoding and when do you use it?",
             "a": "Converts a categorical column with N unique values into N binary columns (0 or 1). Used when the categories have no ordinal relationship (e.g. city names, housing types). Most ML models can't use raw strings."},
            {"q": "What is the difference between normalization and standardization?",
             "a": "Normalization (MinMaxScaler): scales values to [0, 1]. Standardization (StandardScaler): transforms to mean=0, std=1. Use standardization when the distribution is approximately normal; normalization when you need bounded values (neural networks with sigmoid outputs)."},
        ],
        "medium": [
            {"q": "What is binning and when would you apply it to the California housing dataset?",
             "a": "Binning converts a continuous variable into discrete bins (categories). For house age, you might create bins: new (<5 years), mid-age (5-30), old (30+). Useful when the relationship between the feature and target is non-linear and binning the feature lets a linear model approximate it."},
            {"q": "What is feature interaction and how would you create one for house price prediction?",
             "a": "Combining two features to create a new one that captures their joint effect. Example: rooms_per_person = total_rooms / population. A large house in a crowded area is less valuable than in a sparse area — this ratio captures that interaction explicitly."},
            {"q": "What is the curse of dimensionality and how does it affect feature engineering?",
             "a": "As the number of features grows, the data becomes sparse in high-dimensional space — models need exponentially more data to generalize. Feature selection (drop irrelevant features) and dimensionality reduction (PCA) counteract this."},
        ],
        "hard": [
            {"q": "Explain the difference between filter, wrapper, and embedded feature selection methods.",
             "a": "Filter: rank features by statistical tests (correlation, chi-square, mutual information) independent of the model — fast but model-agnostic. Wrapper: train models on feature subsets (RFE, forward selection) — accurate but expensive. Embedded: feature selection during training (Lasso L1 zeroes coefficients, Random Forest feature importance) — best tradeoff."},
            {"q": "What is target encoding and what is the risk of using it without cross-validation?",
             "a": "Target encoding replaces a categorical value with the mean target value of that category. Risk: data leakage — if calculated on the full training set, each row's own value influences its encoding, causing overfitting. Use cross-validation target encoding (compute from out-of-fold data only)."},
        ],
    },

    # ─── CLASSIFICATION METRICS (network-intrusion-detection) ─────────────────

    "classification_metrics": {
        "easy": [
            {"q": "What is precision and recall?",
             "a": "Precision = TP / (TP + FP) — of all predicted positives, how many were actually positive? Recall (sensitivity) = TP / (TP + FN) — of all actual positives, how many did we catch? In intrusion detection, recall matters more — missing an attack is worse than a false alarm."},
            {"q": "What is the F1-score?",
             "a": "The harmonic mean of precision and recall: F1 = 2 * (P * R) / (P + R). It balances both metrics into one number. Useful when class distribution is imbalanced — accuracy alone is misleading."},
            {"q": "What is AUC-ROC?",
             "a": "Area Under the Receiver Operating Characteristic curve. ROC plots True Positive Rate vs False Positive Rate at various thresholds. AUC = 1 is perfect; 0.5 is random guessing. AUC-ROC measures the model's ability to distinguish classes across all thresholds, not just at the default 0.5 cutoff."},
        ],
        "medium": [
            {"q": "What is the difference between micro-average and macro-average F1 in multi-class classification?",
             "a": "Macro-average: compute F1 per class, take unweighted average — treats all classes equally regardless of size. Micro-average: aggregate TP/FP/FN across all classes, then compute F1 — dominated by the majority class. For imbalanced intrusion data: macro-average is more informative."},
            {"q": "What does a confusion matrix look like for 5-class intrusion classification and what can you read from it?",
             "a": "A 5×5 matrix (Normal, DoS, Probe, R2L, U2R). Diagonal = correctly classified. Off-diagonal = misclassifications. You can see: which attack types are confused with each other (R2L often confused with U2R), and which are confused with normal (false negatives — dangerous misses)."},
            {"q": "What is balanced accuracy and when is it better than standard accuracy?",
             "a": "Balanced accuracy = average recall per class = (1/K) Σ recall_k. On imbalanced datasets where one class (e.g. Normal traffic) dominates, standard accuracy can be 95% while the model misses all rare attack types. Balanced accuracy penalizes poor recall on minority classes."},
        ],
        "hard": [
            {"q": "Explain the precision-recall tradeoff and how you would choose the classification threshold for this IDS.",
             "a": "Lowering the threshold → more traffic flagged as attacks → higher recall (catch more attacks) but lower precision (more false alarms). Raising threshold → fewer false alarms but may miss attacks. For an IDS in a critical system (nuclear plant): maximize recall (low threshold). For a noisy enterprise network: balance to avoid alert fatigue. Plot the precision-recall curve and pick the threshold at the F1 peak, or set based on operational constraints."},
            {"q": "What is Cohen's Kappa and why is it more reliable than accuracy for imbalanced multi-class problems?",
             "a": "Cohen's Kappa = (accuracy - expected_accuracy) / (1 - expected_accuracy). It measures agreement between predictions and true labels, correcting for the agreement expected by chance. A model that always predicts 'Normal' achieves high expected_accuracy by chance. Kappa penalizes this — a model with Kappa=1 is perfect, Kappa=0 is no better than random."},
        ],
    },
}
